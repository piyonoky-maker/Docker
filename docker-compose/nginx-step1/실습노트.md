## nginx-step1 실습

```bash
docker compose up -d

curl -I http://localhost:80

# curl.exe -I http://localhost:80 powershell


ps -ef | grep nginx

sudo lsof -i :80

docker ps

docker compose down

docker ps

#👉 서비스 상태 + 실제 동작 여부 한 번에 확인
systemctl status nginx
curl -I http://localhost

```
